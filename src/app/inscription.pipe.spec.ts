import { InscriptionPipe } from './inscription.pipe';

describe('InscriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new InscriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
